package com.cb;

import org.lwjgl.input.Keyboard;

public class Game {
	
	public Game()
	{
		
	}
	
	public void run()
	{
		boolean running = true;
		while(running)
		{
			if (Keyboard.isKeyDown(Keyboard.KEY_ESCAPE))
			{
				running = false;
			}
		}	
	}
}
